<!DOCTYPE HTML> 
<html lang="en">
{{>head}}
<body>
  {{>header}}
  <div class="container">
  <div class="row">
    {{!--
      {{log this}}
    --}}
    <div class="col-sm-12">
      {{{ body }}}
    </div>
  </div>
  </div>
  {{>footer}}
</body>
</html>